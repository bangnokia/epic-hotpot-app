import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.epichotpot.app',
  appName: 'Epic HOTPOT',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
